<!-- src/Resources/app/administration/src/module/sw-cms/elements/special-table/component/sw-cms-el-special-table.html.twig -->
{% block sw_cms_element_special_table %}
    <div class="sw-cms-el-special-table">
        <div v-if="tableData.rows.length > 0" class="special-table-preview">
            <table class="table preview-table"
                   :class="{
                       'table-striped': tableData.striped,
                       'table-bordered': tableData.bordered,
                       [`table-${tableData.style}`]: tableData.style !== 'default'
                   }">
                
                <thead v-if="tableData.showHeader && tableData.headers.length > 0">
                    <tr>
                        <th v-for="(header, index) in tableData.headers" :key="index">
                            {{ header }}
                        </th>
                    </tr>
                </thead>
                
                <tbody>
                    <tr v-for="(row, rowIndex) in tableData.rows" :key="rowIndex">
                        <td v-for="(cell, cellIndex) in row" :key="cellIndex">
                            {{ cell }}
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
        
        <div v-else class="special-table-placeholder">
            <div class="alert alert-info">
                {{ $tc('specialBlocks.table.noDataAvailable') }}
            </div>
        </div>
    </div>
{% endblock %}